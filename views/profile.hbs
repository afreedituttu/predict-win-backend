
    <section>
        <div class="title">Profile</div>
        <div class="profile-card">
            <div class="image">
                <img src="./profile-pic.png" alt="">
            </div>
            <div class="content">
                <form action="">
                    <input type="text" value="{{user.name}}" placeholder="name">
                    <input type="text" value="{{user.team}}" placeholder="name">
                    <input type="text" value="{{user.branch}}" placeholder="name">
                    <input type="text" value="{{user.password}}" placeholder="name">
                    <div class="options">
                        <div class="delete"><a href="/delete">Delete</a></div>
                        <input type="submit" value="Update">
                    </div>
                </form>    
            </div>
        </div>
    </section>

    <br><br><br>
    <br><br><br>
